{
  "name": "coincapbot",
  "version": "1.0.0",
  "description": "A simple slackbot that will post the prices of crypto prices",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js & node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jake Cooley",
  "license": "ISC",
  "dependencies": {
    "anychart-nodejs": "^1.3.8",
    "aws-sdk": "^2.276.1",
    "express": "^4.16.3",
    "fs": "^0.0.1-security",
    "jsdom": "^11.11.0",
    "path": "^0.12.7",
    "request-promise": "^4.2.2",
    "rimraf": "^2.6.2",
    "slackbots": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
